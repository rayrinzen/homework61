doctype html
html
  head
    title= user.name
    link(rel="stylesheet", href="/styles.css")
  body
    .container
      h1 #{user.name}
      p Email: #{user.email}
      a.back-link(href="/users") ← Назад до списку

