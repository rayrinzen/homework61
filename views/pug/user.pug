doctype html
html
  head
    title= user.name
    link(rel="stylesheet", href="/styles.css")
    link(rel="icon", href="/favicon.ico")
  body(class=theme)
    .container
      h1 #{user.name}
      p Email: #{user.email}
      a.button.back(href="/users") ← Назад до списку





